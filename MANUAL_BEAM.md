# ビームプロファイラ画像数値化ツール マニュアル

このツールは、ビームプロファイラのスクリーンショット画像から、数値データ（M^2、広がり角、ビーム径など）とグラフのプロット点を抽出し、Excelファイルとして保存するツールです。

## 1. 準備（環境構築）

ツールを実行するには、以下のライブラリが必要です。

```bash
pip install numpy opencv-python pandas openpyxl easyocr
```

※ OCRエンジン（easyocr）の初回実行時には、学習モデルのダウンロードが行われるため、インターネット接続が必要です。

## 2. ツールの使い方

### GUI（画面）で使う場合

以下のコマンドでツールを起動します。

```bash
python beam_digitizer.py
```

1. **画像を選択**: 「画像を選択」ボタンを押し、解析したいビームプロファイラの画像（PNG, JPG等）を選びます。
2. **解析実行**: 「解析実行 & Excel保存」ボタンを押します。
3. **保存先指定**: Excelファイルの保存場所と名前を指定します。
4. **完了**: 解析が終わると完了メッセージが表示されます。

### CLI（コマンド）で使う場合

自動化などで直接実行したい場合は、引数に画像パスを渡します。

```bash
python beam_digitizer.py [画像パス]
```

実行したフォルダに `output.xlsx` が作成されます。

## 3. 出力データの内容

保存されるExcelファイルには2つのシートが含まれます。

- **Beam Parameters シート**:
    - 画像左側の「Results」テーブルから抽出した数値（M^2, Divergence, Waist Width, Waist Location等）が一覧表示されます。
- **Graph Data シート**:
    - 画像右側の「Measured Caustic Display」グラフから抽出したX（オレンジ）とY（青）のデータポイントが、数値（Z位置[mm]、ビーム径[um]）に変換されてリストアップされます。

## 4. 注意点

- 画像のレイアウトが大きく異なる場合、正しく数値を読み取れないことがあります。提供されたサンプル画像に近い形式を推奨します。
- グラフの数値変換は、一般的な軸の範囲に基づいた計算を行っています。絶対的な精度が必要な場合は、元のCSVデータ等との照合を行ってください。
- OCRの性質上、文字の誤認識（0をOと読み間違える等）が発生する可能性があります。Excel保存後に内容を確認してください。
