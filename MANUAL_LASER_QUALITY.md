# レーザ品質管理システム マニュアル

このツールは、ビームプロファイラの測定結果画像から自動的に数値を抽出し、複数のデータをまとめて工程管理能力（Cp/Cpk）の分析まで行うツールです。

## 1. 準備（環境構築）

ツールを実行するには、以下のライブラリが必要です。

```bash
pip install numpy opencv-python pandas openpyxl easyocr
```

※ OCRエンジン（easyocr）の初回実行時には、インターネット接続が必要です。

## 2. ツールの使い方

### GUI（画面）で使う場合

以下のコマンドでツールを起動します。

```bash
python laser_quality_manager.py
```

1. **データ選択**:
   - 「ファイルを選択」で複数の画像ファイルを選ぶか、
   - 「フォルダを選択」で画像が入ったフォルダを丸ごと指定します。
2. **解析実行**: 「一括解析 & Excel保存」ボタンを押します。
3. **保存先指定**: 集計結果を保存するExcelファイルの名前と場所を指定します。
4. **解析開始**: 解析（OCR処理）が始まります。1枚あたり数秒〜数十秒かかる場合があります。
5. **完了**: 解析が終わると完了メッセージが表示されます。

## 3. 出力データと分析内容

保存されたExcelファイルには以下のシートが含まれます。

- **Raw Data シート**:
    - 各画像から抽出されたすべての数値が、ファイル名・日時とともに一覧表になります。
- **Process Capability シート**:
    - 主要なパラメータ（M^2、ビームウエスト径、広がり角など）について、以下の統計量を自動計算します。
    - **平均 (Mean)** / **標準偏差 (StdDev)**
    - **最大値 (Max)** / **最小値 (Min)**
    - **工程能力指数 (Cp/Cpk)**: 予め設定された規格（USL/LSL）に基づき、工程の安定性を評価します。

## 4. 便利な機能

- **一括処理**: 過去の測定画像をまとめて一つのExcelにできるため、月次レポートの作成などが劇的に効率化されます。
- **傾向把握**: 生データが時系列（タイムスタンプ）で並ぶため、ビーム品質の経時変化をグラフ化するのに適しています。

## 5. 注意点

- **規格値（USL/LSL）**: 現在、デモ用に一般的な規格値がプログラム内に設定されています。実際の製品仕様に合わせた調整が必要な場合は、担当者へお問い合わせください。
- **読み取り精度**: 画像内の文字が潰れている場合、誤認識が発生することがあります。重要な数値はExcel保存後に必ず確認してください。
