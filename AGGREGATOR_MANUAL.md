# 課題連絡票集計システム マニュアル

このツールは、個別の「課題連絡票」Excelファイルから情報を自動的に抽出し、「集計用」のExcelファイルに一覧として追記するプログラムです。

## 1. 準備（環境構築）

Pythonがインストールされている環境で、以下のライブラリをインストールしてください。

```bash
pip install openpyxl
```

## 2. ツールの使い方

1.  プログラムを実行します。
    ```bash
    python issue_aggregator.py
    ```
2.  画面が表示されたら、「1. 個別ファイルを選択してください」のボタンを押し、転記したいExcelファイルをすべて選択します（複数を一度に選べます）。
3.  「2. 集計用ファイルを選択してください」のボタンを押し、追記先のExcelファイル（集計用）を選択します。
4.  「集計を実行する」ボタンを押すと、自動的にデータが転記されます。

## 3. データの転記ルール

以下のセルの情報が抽出され、集計用ファイルの新しい行に追記されます。

| 項目 | 個別ファイル（元） | 集計用ファイル（先） |
| :--- | :--- | :--- |
| No | (自動採番) | A列 |
| 発生日 | C4セル | C列 |
| 機種 / 号機 | C5セル | E列 |
| 発生事象 | C6セル | G列 |
| 報告者 / 起案者 | E4セル | I列 |
| 暫定対応 / 対策方法 | E8セル | J列 |
| 作業区分 | C8セル | K列 |

## 4. 注意点

- 集計用ファイルが開かれている状態だとエラーになる場合があります。閉じてから実行してください。
- ファイルのレイアウトが大幅に変更されると、正しく抽出できなくなる可能性があります。
