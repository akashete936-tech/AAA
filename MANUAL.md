# ニュースまとめパワーポイント作成ツール マニュアル

このツールは、指定したニュースサイトのURLから記事のタイトルと本文を抽出し、自動的にパワーポイント（PPTX形式）のスライドを作成するPythonスクリプトです。

## 1. 準備（環境構築）

Pythonがインストールされている環境で、以下の手順で必要なライブラリをインストールしてください。

コマンドプロンプトやターミナルを開き、以下のコマンドを入力します。

```bash
pip install python-pptx newspaper3k lxml_html_clean
```

※ もしエラーが出る場合は、`pip3` を試してみてください。

## 2. ツールの使い方

`.py` ファイルを実行するには、コマンドプロンプトやターミナルで以下の形式でコマンドを入力します。

### 基本的な使い方

```bash
python news_to_pptx.py [記事のURL]
```

（例）
```bash
python news_to_pptx.py https://example.com/news/article1
```

実行が完了すると、同じフォルダ内に `news_summary.pptx` という名前でパワーポイントファイルが保存されます。

### 複数の記事を一度にまとめる

URLをスペース区切りで複数並べることで、複数の記事を1つのパワーポイントにまとめることができます。

```bash
python news_to_pptx.py URL1 URL2 URL3
```

### 出力ファイル名を指定する

`-o` オプションを使うことで、保存されるファイル名を変更できます。

```bash
python news_to_pptx.py URL1 -o 2024ニュースまとめ.pptx
```

## 3. 注意点

- サイトによっては、スクレイピング（情報の抽出）を禁止している場合があります。各サイトの利用規約を確認してください。
- 有料記事やログインが必要な記事は正しく取得できない場合があります。
- 本文が非常に長い場合、スライド内では冒頭の500文字程度に省略されます。

## 4. プログラムの内容（初心者向け解説）

- `fetch_article`: インターネットからニュースの内容を取ってくる関数です。
- `create_presentation`: 取ってきた内容をパワーポイントの形に整えて保存する関数です。
- `main`: あなたの命令を受け取って、上記の処理を順番に実行するメインの部分です。
